{
  "name": "the-right-key-game",
  "version": "1.0.0",
  "description": "idk",
  "homepage": "https://markiianperliak.github.io/the-right-key-game/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel src/index.html",
    "build": "parcel build src/index.html --no-cache --public-url /the-right-key-game/",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "author": "Markiian",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.14.4"
  }
}
